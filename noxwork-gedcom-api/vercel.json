{
  "version": 2,
  "buildCommand": "npm run prisma:generate && npm run build && npx prisma migrate deploy",
  "builds": [
    {
      "src": "dist/serverless.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/serverless.js"
    }
  ]
}
